# 2026-05-20

- 已确认这个 `course-schedule` 需要走 `database + puppeteer` 路线。
- 已拆出 `src/services`、`src/render`、`src/commands`、`src/utils` 的骨架。
- 已接入基础入口和数据库表定义。
- 已补一个最小可渲染的 HTML 模板，后续可直接替换成 AstrBot 风格出图。
- 已补 WakeUp 导入主链路：分享口令提取、接口拉取、课程入库。
- 已补个人课表、群课表、本周排行的最小查询与出图逻辑。
- 已补 `.ics` 导入主链路：支持 ICS 文本、ICS 链接，且兼容尝试读取消息里的 `.ics` 附件链接。
- 已把模板往 AstrBot 风格推进一版：头像、状态块、左侧强调色、分层卡片。
- 已跑 `npm run build`。当前 `course-schedule` 没再报新的 TS 错误；整仓 build 仍被 `external/kelebot-sponsor/src/types.ts` 的既有 `console` 类型冲突拦住。
- 当前状态：`database + puppeteer + WakeUp + ICS` 已串起来，但 `.ics` 的高级递归规则、平台特定附件细节、以及更像 AstrBot 的精细排版还可以继续增强。
